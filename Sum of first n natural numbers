#include <stdio.h>
int main()
{
    /* program to print the sum of firstn natural nunbers */

    int i, n, sum = 0;
    printf("Enter the number\n");
    scanf("%d", &n);

    for (i = 0; i <= n; i++)
    {
        sum = sum + i;
    }
    printf("The sum is %d", sum);

    return 0;
}
