#include <stdio.h>

int main (){
    
    int i,n, r;
    
    printf("Enter a number:\n");
    scanf("%d", &n);
    
    for (i = 10; i >= 1; i--){
        r = n*i;
    printf("%d x%d = %d\n", n, i,r);
    }
    return 0;
}
