#include <stdio.h>

int main()
{
    int num;
    printf("Enter the year:\n");
    scanf("%d", &num);

  /*  if (num % 400 == 0)
    {
        printf("Leap year");
    }
    else if (num % 100 == 0)
    {
        printf("not a leap year");
    }
    else if (num % 4 == 0)
    {
        printf("Leap year");
    }
    else
    {
        printf("not a leap year");
    }
    */
    
    if ((num%400== 0)|| (num%4==0 && num%100 != 0)){
        printf("Leap year");
    }else{
        printf("Not a leap year");
    }
    
    
    return 0;
}
